# Local preview only (make preview). Not applied to any cluster.
apiVersion: 1
providers:
  - name: obs-as-code
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
